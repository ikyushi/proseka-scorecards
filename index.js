const { createCanvas, loadImage } = require('canvas');
const canvas = createCanvas(740, 740);
const ctx = canvas.getContext('2d');

// 4444ca is the color of the background for the difficulty number (that sounds really confusing)
